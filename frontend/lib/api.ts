// Typed client for the TrustLine backend (Phase 2 REST API).
// Phase 0 scaffold: base URL only; endpoint calls added in Phase 3.

export const API_BASE_URL =
  process.env.NEXT_PUBLIC_API_BASE_URL ?? "http://localhost:8787";
