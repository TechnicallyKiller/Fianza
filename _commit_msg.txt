demo: /demo reality-check bar + fix age-source bug in independence engine

- demo page: same claimed-to-counted reality bar as /underwrite for the
  honest/sybil cards.
- independence: found and fixed a real bug testing this live -- the DB-graph
  path used first_seen_time from OUR OWN ingest window as account age, so a
  payer active for months looked brand new until the graph backfilled that
  far back. Now takes max(db age, Horizon true account-creation age).
- adversarial catalog re-verified after the fix: all 6 scenarios still pass.
  A1 self-pay stays caught -- an earlier averaging-based fix attempt was
  reverted because it weakened real Sybil detection just to fix a display
  issue.

Known gap, documented not fixed: the original /demo honest-agent's seed
payer wallets are single-purpose (only ever paid the agent, zero other
on-chain activity), so the diversity signal correctly scores them near-zero
and that specific preset now shows denied even though the agent is
legitimately honest. Defensible model behavior (thin evidence) but the demo
preset needs payers with real external activity to show honest-passes
cleanly. /underwrite's live-attacker catch is unaffected and remains the
strongest live artifact.
