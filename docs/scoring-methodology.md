# Scoring methodology

Composite revenue-coverage score: indexed on-chain x402 revenue + one
Reclaim-verified off-chain revenue figure, banded into tiers that map to credit
limits and a fixed APR. Filled in during Phase 1 (on-chain `revenue_math`) and
Phase 2 (off-chain `scoring/`), kept consistent across both.
