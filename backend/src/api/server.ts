// api/ — REST endpoints the frontend needs (Fastify).
//
// Phase 2: expose endpoints for agent revenue, proof submission, score/limit,
// vault state, and credit-line actions, wiring indexer/ + zktls/ + scoring/ +
// signer/ together.
//
// Phase 0 scaffold: entrypoint placeholder, no server booted yet.

export {};
